# Atoms — Universal Block Vocabulary

`schema.yaml` defines all atoms. Each atom declares surface compatibility via `works_on`, `degraded_on`, and `incompatible_on` tags.

Renderers for each surface live in `../renderers/`.
